---
import BaseLayout from '../layouts/BaseLayout.astro';
import { t } from '../i18n/utils';

const locale = 'zh';
const tr = t(locale);
---

<BaseLayout title={tr('about.title')} description={tr('about.title')} locale={locale}>
  <div class="detail">
    <h1 class="page-title">{tr('about.title')}</h1>
    <p>我在芝加哥，在科技行业上班。业余做独立应用，也拍视频。</p>
    <p>
      应用是自己想用才做的小工具，都在作品页。视频拍家庭旅行、上班日常、相机测评，还有一些查资料查上头的选题——发在
      YouTube、小红书和 B 站。
    </p>
    <p>更早以前在微博做过一段时间认证博主，写些段子；再往前有一个博客。</p>
  </div>
</BaseLayout>
